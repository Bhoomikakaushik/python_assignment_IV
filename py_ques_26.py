# Ques - 26 To display all numbers of a list with power of 2 using lambda function 

numbers = [1,2,3,4,5]

answer = list(map(lambda x:x**2 , numbers))
print(f"List by taking power of 2 of each number : {answer}")